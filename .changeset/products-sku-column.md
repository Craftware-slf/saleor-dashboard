---
"saleor-dashboard": minor
---

Add a SKU column to the dashboard Products list. Products with a single variant show that variant's SKU; multi-variant products show the shared base SKU plus a variant count; products without a variant SKU show "-". The column is visible by default and can be toggled in the column picker.
