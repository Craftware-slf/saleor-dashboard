---
"saleor-dashboard": patch
---

Align the "Publish on <channel>" button with the Availability card's channel rows (FEAT-144 follow-up). The button is a bare sibling of that card in the sidebar, so without the card's own horizontal padding it ran the full card width — noticeably wider than everything above it, with its helper text flush against the panel edge.
